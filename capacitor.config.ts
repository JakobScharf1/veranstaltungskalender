import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.brombeerland.veranstaltungskalender',
  appName: 'veranstaltungskalender',
  webDir: 'dist'
};

export default config;
